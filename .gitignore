# MindfulNest project — defensive .gitignore.
#
# This repo lives inside Dropbox (.git itself is xattr-tagged
# com.dropbox.ignored=1 to prevent Dropbox from corrupting the index). Be
# conservative about what's staged: working .docx files, secrets, generated
# binaries, and Office lock files MUST NOT be tracked.

# ============ OS / editor / Dropbox metadata ============
.DS_Store
Icon?
._*
.dropbox
.dropbox.cache/
.dropbox.attr
*.icloud

# ============ Office / Word lock files ============
.~lock.*#
~$*

# ============ Node / build artifacts ============
node_modules/
dist/
.vite/
*.tsbuildinfo
playwright-report/
test-results/

# ============ Python ============
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/

# ============ Secrets / credentials ============
# Anything mentioning keys, secrets, credentials must NEVER be tracked.
API_KEYS_MASTER*
*credentials*
*secret*
.env
.env.*
!.env.example
# LD-227 Phase 1 (2026-05-08): Production/tools/lib/credentials.py is the
# centralized credential-LOADING library — it contains env-var name strings
# and parsing logic but NO credential VALUES. Allow this one path so the
# library can be tracked + cross-machine synced. Any new file matching
# *credentials* in OTHER locations remains ignored by default.
!Production/tools/lib/credentials.py

# ============ Project-specific working state ============
# Directus offline write queue — local-only fallback.
pending_directus_writes.json
pending_directus_writes.resolved_*.jsonl

# Daily/post-cutover snapshots (M5).
.backups/

# Auto-memory + checkpoint state (kept out of git for now; .auto-memory/
# tracks Claude's persistent memory, .mn-context tracks session checkpoints).
.auto-memory/
.mn-context/

# Per-session shadow logs.
~/.claude/mindfulnest-cache/

# Claude Code session helper files (drafted then deleted).
_session*.py
_session*.json

# Hook shadow logs / activity drafts.
hook_shadow_log.jsonl

# ============ Dropbox-staged artifacts that don't belong in git ============
# Dropbox-style stage scripts at root.
.drop_staging.py
.lds_for_inventory.json
.task_id_*.txt
.task_module_exit_state.json
.top_20_backfill_candidates.json
.wave_*.py
.wave_*.json

# ============ Large binary working assets (defensive — surface to Kim if
# needed in repo) ============
# Audio working masters
*.wav
*.aif
*.aiff
*.flac
*.m4a
# Generated MP3s + MP4s in Event dirs (we'll selectively whitelist key deliverables)
Production/Event_*/animation_clips/
Production/Event_*/preserved_winners/
Production/Event_*/clips_dir/
Production/Event_*/sources/
Production/Event_*/cropped/
Production/Event_*/masters/
Production/Event_*/_temp_*/
Production/Event_*/_sandbox/
Production/Event_*/.backups/
*.mp3
*.mp4
*.mov

# Image working assets
*.png
*.jpg
*.jpeg
*.webp

# But DO allow the v58 golden + v59 single-file HTML deliverables.
!Production/Event_*/storyboard_v*_prod.html
!Production/Event_*/storyboard_v*_GOLDEN_BACKUP_*.html
!Production/Event_*/storyboard_v*_prod.html.sha256
!Production/Event_*/storyboard_v*_GOLDEN_BACKUP_*.html.sha256

# ============ Working .docx / large doc binaries (Kim edits these in Word) ============
# .docx files are NOT tracked by git per Rule 3 read-before-write semantics —
# Dropbox is the source of truth for these. If we want to track a snapshot,
# whitelist the specific file explicitly.
*.docx
*.xlsx
*.pptx
*.pages

# ============ Backup files (Word, ad-hoc) ============
*.bak
*.bak_*
*.backup
*~
*.bak.*

# ============ Runtime state (audit Item 1, severity CRITICAL — 2026-05-07) ============
# Per SILENT_DEFERRALS_AUDIT_20260507.md Item 1 + Kim authorization 2026-05-07.
# Runtime byproducts (server PIDs, fixture locks, runtime JSON state) that previously
# left the working tree dirty and tripped session-start STOP checks.
*.pid
.state.lock
*.L.json
beat_generator_state.json
production_state.json

# ============ LD-505 Pre-Phase-A Triage additions (2026-05-09 by Kim defaults D-1/D-3/D-5/D-6/D-8) ============
# Per LD505_PRE_PHASE_A_TRIAGE_AUDIT_20260510.md §1 disposition rollup.
# Already-tracked files under these patterns continue to be tracked
# (gitignore is not retroactive); these patterns prevent NEW pollution.

# D-1: Claude Code project-local skills are runtime artifacts (not source)
.claude/skills/

# D-3: orphan root-level skill dirs (no .claude/skills/ counterpart)
case-study-skill/
email-nurture/
website-copy-skill/
*.skill/

# D-5: binary installer accidentally committed to Event_1 working tree
Production/Event_1/Claude.dmg
*.dmg

# D-6: storyboard-v2 e2e *.ts duplicates of tooling repo (tooling is canonical per LD-505)
Production/tools/storyboard-v2/e2e/*.ts

# D-8: 22 root-level content directories — working/content material, not source
Marketing/
Old Versions/
Research/
Session Logs/
Module Demos/
Parent Coach/
App Design/
Business/
Clinical/
Cropper/
Canon/
Arc Skeletons/
Claude Code History backup 2026-04-15/
archive/
.deploy_backups/
content-lockfiles/
scripts/
video_pipeline/
Claude ElevenLabs Phase B Module Sounds/

# Tier 3: bulk Production/Event_* output (327 untracked entries; whitelist exceptions
# for currently-tracked files: production_state.json + storyboard_v59_prod.html + .sha256)
Production/Event_*/
!Production/Event_*/production_state.json
!Production/Event_*/storyboard_v59_prod.html
!Production/Event_*/storyboard_v59_prod.html.sha256
!Production/Event_*/storyboard_v58_prod_GOLDEN_BACKUP_20260502_160345.html
!Production/Event_*/storyboard_v58_prod_GOLDEN_BACKUP_20260502_160345.html.sha256

# Tier 6D: loose state/temp/log/conflicted files at repo root
*conflicted copy*
desktop (2).ini
desktop.ini
server_restart.log
test_write.txt
feedback.json
fal_lora_result.txt
mindfulnest_full_schema.sql
storyboard_sequence_2026-04-16.json
crop_manifest_M1_e1.json
crop_manifest_M1_e1 (1).json
animation_selections.json
animation_selections (1).json
dashboard-ops-eval-viewer.html
ziR6fyTf/
Production/tmp*.tmp

# Additional Claude Code session state (not project source)
.claude/LATEST_CHECKPOINT.json
.claude/hooks/
.claude/scheduled_tasks.lock
.claude/session_checkpoints/
.claude/session_scratchpad.json
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/

# Dot-prefixed variant of pending Directus writes (existing pattern was un-prefixed)
.pending_directus_writes*

# Production/ root-level working/content directories (per LD-505 audit Tier 5/6 leftovers)
Production/Grizzle/
Production/Guide_Bird/
Production/Tessa/
Production/Willow/
Production/Milestones/
Production/RUNBOOKS/
Production/Phase_A_Office_Generation/
Production/Phase_B_Style_Guide/
Production/_audit_workspace/
Production/_handoff_workspace/
Production/_previews/
Production/_sandbox_archive_April8_2026/
Production/_session_*
Production/api/
Production/audio-producer-skill/
Production/skills/
Production/specs/
Production/stitch_editor_cache/
Production/test_harness/
Production/validators/
Production/video-producer.skill/

# Production/ root-level standalone working files (not source)
Production/audio_review.html
Production/start_production_server*.command
Production/species_swap.py
Production/AUDIO_STRIPPING_DELIVERABLES.txt
Production/DIFF_REPORT_MODULE_PLAN_v2_1_to_v2_2.txt
Production/LOCKED_DECISIONS_CATALOG_v1.txt
Production/MODULE_CONTEXT_REGISTRY_v1.json
Production/PROD_SCRIPTS_REGISTRY_TEST_REPORT_April14_2026.txt
Production/Quick_Reference_Card_v1.txt
Production/TOOLS_AUDIT_SUMMARY.txt
Production/Quick_Reference_Card_v2.md
Production/storyboard_browser_smoke.png
Production/storyboard_browser_smoke_after.png

# Root-level loose binary/asset
BLENDER_3D_COST_BREAKDOWN_SPREADSHEET.csv

# More .skill/ dirs that *.skill/ pattern didn't catch
storyboard-producer.skill/
dashboard-ops.skill/
dashboard-gate.skill/
audio-producer-skill/

# Production/tools/ untracked working subdirs (not source)
Production/tools/_option_c_prototype_exec/
Production/tools/storyboard-v2/

# .skill / weird-name FILES (not just dirs — patterns above with trailing / only matched dirs)
*.skill
ziR6fyTf

# Production/tools state + output files (working artifacts, not source)
Production/tools/magic_clip_registry.json
Production/tools/sample_animation_*
Production/tools/storyboard_image_map_*.json
Production/tools/stitch_editor.html
Production/tools/stitch_editor_state.json
Production/tools/stitch_editor_state.lock
Production/tools/stitch_editor_template.html
Production/tools/tests/TIER3_BROWSER_E2E_REPORT_*.md

# Production/ root-level working files
Production/audio_review_local.html
Production/character_subjects.json
Production/kling_check_status.sh
Production/m3 phase b meditation script.txt
Production/module_M[0-9]_config.json
Production/module_json_builder_eval_results.html
Production/skeleton_beats_*.html
Production/skeleton_to_beats_review.html
Production/shot6_*.py

# Production/ root-level subdirs (untracked working content)
Production/contracts/
Production/dashboards/
Production/e2e/
Production/exports/
Production/github_actions/
Production/handoffs/
Production/migrations/
Production/tests/fixtures/
