# V59 architectural-fix Wave 1 (F-CI-001) — canonical runtime dep list for
# production_server.py + sibling tools modules exercised by the Playwright
# e2e gate. Per LD PRODUCTION_TOOLING_REQUIREMENTS_TXT_V1.
#
# Audit method: grep -hE '^import [A-Z]|^from [A-Z]' Production/tools/*.py +
# Production/lib/*.py, filtered against the Python stdlib. Production_server
# imports `yaml` (5267, 5387) + `PIL` (820, 847, 5793, 8123, 8268). The
# kling_startend_pipeline.py + ffmpeg_utils.py + lipsync_sender.py modules
# (imported by production_server) add no further non-stdlib deps.
#
# numpy added 2026-05-19 (P2 of LD-505 Phase C cleanup): needed by
# magic_compositor.py + background.py's /api/magic/resolve_bg endpoint.
# Without it, the endpoint silently 500s on first user click (audit C4-2).
# Out of scope for this requirements file (and tracked separately if needed):
#   - requests / cv2 / torch / sklearn etc. used by NO runtime-loaded
#     handler modules; the lazy-import probe at run_server emits a structured
#     "[startup:capabilities]" line when any soft dep is missing.
#   - pip-tools / lockfile (`requirements.lock`) — deferred per spec §10
#
# CI workflow installs from this file via `pip install -r Production/tools/requirements.txt`.

PyYAML>=6.0
Pillow>=10.0
numpy>=1.24
scipy>=1.10
python-dateutil>=2.8
